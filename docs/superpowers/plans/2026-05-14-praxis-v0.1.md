# Praxis v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 Praxis plugin v0.1 — 一个可被 Claude Code 装载、包含 9 个 skill 的本地 plugin,所有 skill 遵循统一 5 段骨架,think 写到完整版,其它 8 个写到结构完整、内容简短的可用版。

**Architecture:** Claude Code plugin,无构建步骤,纯 markdown + 一份 plugin.json。目录布局: `.claude-plugin/plugin.json` + `skills/<slug>/SKILL.md` × 9 + `README.md`。

**Tech Stack:** Markdown,Claude Code Plugin v1 spec,zsh(本地验证)。

---

## ⚠️ 关于 git 操作

用户明确要求 git init 等通知再做。**本 plan 所有任务不包含 `git init` / `git add` / `git commit` 步骤**。每个任务以"自查命令"收尾代替 commit。git 化作为 plan 完成后的独立步骤,由用户触发。

## ⚠️ 关于"最简可用版"的解释

spec 第 3 节要求 5 段骨架,第 7 节"最简版"列出 4 段。本 plan 按"5 段都齐,但每段内容简短"解释。

---

## File Structure

```
Praxis/
├── .claude-plugin/
│   └── plugin.json                ← Task 1
├── skills/
│   ├── think/SKILL.md             ← Task 2 (完整模板)
│   ├── plan/SKILL.md              ← Task 3
│   ├── write-code/SKILL.md        ← Task 4
│   ├── review/SKILL.md            ← Task 5
│   ├── debug/SKILL.md             ← Task 6
│   ├── design-ui/SKILL.md         ← Task 7
│   ├── read/SKILL.md              ← Task 8
│   ├── write-prose/SKILL.md       ← Task 9
│   └── learn/SKILL.md             ← Task 10
├── README.md                      ← Task 11
└── docs/                          ← 已存在
    └── superpowers/{specs,plans}/
```

**职责分配**:
- `plugin.json`: plugin 元信息(name / version / description)
- 每个 `SKILL.md`: 单一职责 — 一个 skill 的目标 + 约束 + 产出契约
- `README.md`: 哲学陈述 + 9 个 skill 速查表 + 安装说明 + 与 superpowers 对比

---

## Task 1: 初始化 plugin 骨架

**Files:**
- Create: `.claude-plugin/plugin.json`
- Create: `skills/` (空目录,由后续任务填充)

- [ ] **Step 1: 创建 plugin.json**

写入 `/.claude-plugin/plugin.json`:

```json
{
  "name": "praxis",
  "version": "0.1.0",
  "description": "Nine skills that turn raw model capability into precision: clear goals, named constraints, output contracts. Process is left to the model.",
  "author": {
    "name": "qianyu"
  },
  "homepage": "https://github.com/<待定>/praxis",
  "keywords": ["skills", "engineering", "writing", "learning"],
  "license": "MIT"
}
```

注: `homepage` 字段先留 `<待定>` 占位,git init / GitHub repo 创建后由用户填回。

- [ ] **Step 2: 创建 skills 目录**

Run: `mkdir -p skills`
Expected: 静默创建,后续任务在此目录下创建子目录。

- [ ] **Step 3: 自查 — plugin.json 合法 JSON**

Run: `python3 -c "import json; json.load(open('.claude-plugin/plugin.json'))" && echo OK`
Expected: 输出 `OK`。

---

## Task 2: 写 think SKILL.md(完整模板)

**Files:**
- Create: `skills/think/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/think`

- [ ] **Step 2: 写 SKILL.md 完整内容**

写入 `skills/think/SKILL.md`:

````markdown
---
name: think
description: Use when starting any non-trivial task and the goal is fuzzy. Turns a rough idea into a verifiable goal, named constraints, and exposed assumptions before any other skill (write-code, debug, design-ui, write-prose, learn) runs.
---

## When to invoke
- 用户说"我想做 X"但没说怎么判断 X 算做完
- 输入有动词但没有验收方式
- 一个需求能被合理解释成 2 种以上的东西
- 紧接着要调 write-code / debug / design-ui / write-prose / learn,但目标还说不清

## Inputs required
- 一句话粗想法或需求(允许模糊)
- 用户能想到的约束(时间、技术栈、不能动的东西、谁会用)

## Goal
产出一份"清晰目标 + 命名约束"陈述,使下游的 skill 或人能把它当 Inputs required 直接消费,不需要再问"你到底想要什么"。

## Hard constraints
- 不写代码、不动文件、不画实施步骤(那是其它 skill 的事)
- 不接受"差不多""大概""可能"的目标 — 不可验证就重写到可验证为止
- 必须暴露至少 1 条用户没明说的假设
- 输入有 ≥2 种合理解释时,必须列出让用户选,不准自己挑
- 如果输入已经是"可验证目标 + 命名约束",直接说"不需要 think",不强行跑

## Output contract
- 一句话**目标**(包含验收方式,例:"X 完成 = Y 通过")
- 列表式**约束**(必须做的 + 必须不做的)
- 列表式**暴露的假设**(每条以"我假设…"开头)
- 如有歧义,列**可能的解释方向**(2-4 条,让用户选)
````

- [ ] **Step 3: 自查 — 行数 / 段名 / Hard constraints 数量**

Run:
```bash
F=skills/think/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected:
- `Lines:` ≤ 80
- `Sections: 5`(When to invoke / Inputs required / Goal / Hard constraints / Output contract)
- `Constraints:` 在 3-7 之间(本任务为 5)

---

## Task 3: 写 plan SKILL.md

**Files:**
- Create: `skills/plan/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/plan`

- [ ] **Step 2: 写 SKILL.md**

写入 `skills/plan/SKILL.md`:

````markdown
---
name: plan
description: Use after think has produced a verifiable goal and named constraints, when the next step is multi-action work. Decomposes the goal into an ordered task sequence where each task has independent acceptance.
---

## When to invoke
- think 已产出"可验证目标 + 命名约束",但下一步显然不是单一动作
- 用户给出复合需求(包含 ≥3 个独立可验证产物)
- 即将调 write-code / debug,工作明显需要拆步骤

## Inputs required
- 一句话可验证目标
- 命名约束列表
- 已知的资源限制(时间、人力、技术栈)

## Goal
产出有顺序的任务序列,每个任务有独立产物和独立验收方式,使下一步可以按任务粒度交给执行者(人或 skill)而不需要二次澄清。

## Hard constraints
- 不写代码、不动文件
- 每个任务必须有独立验收方式(怎么知道这一项做完了),没有就拆得更细
- 任务数 < 3 不出 plan(直接说"不需要 plan");任务数 > 12 必须分批
- 不为每个任务写实现细节(那是 write-code 的事),只写"做什么 + 验收"

## Output contract
- 编号任务列表(顺序敏感)
- 每个任务: 一句话**产物** + 一句话**验收**
- 任务间显式标注**依赖**(如有)
- 一行**风险与未知**(如有)
````

- [ ] **Step 3: 自查**

Run:
```bash
F=skills/plan/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected: Lines ≤ 80,Sections = 5,Constraints 在 3-7 之间(本任务为 4)。

---

## Task 4: 写 write-code SKILL.md

**Files:**
- Create: `skills/write-code/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/write-code`

- [ ] **Step 2: 写 SKILL.md**

写入 `skills/write-code/SKILL.md`:

````markdown
---
name: write-code
description: Use when there is a clear verifiable goal (and for non-trivial work, a plan), and the next action is producing runnable code. Writes the minimum code that satisfies the goal — no speculative abstractions, no unrequested features.
---

## When to invoke
- 已有清晰目标(由 think 或用户直接给出)
- 工作是产出新代码或修改已有代码使其满足新目标
- 不是在排查 bug(那是 debug)、不是在评判(那是 review)

## Inputs required
- 一句话可验证目标
- 命名约束(包含技术栈、不能动的边界)
- 现有代码上下文(如果是修改)

## Goal
产出能运行、能通过目标对应验收方式的最小代码改动。改动后的代码遵守命名约束,不引入未被要求的能力。

## Hard constraints
- 不写未被要求的功能(YAGNI 刚性化)
- 不为单次使用引入抽象;不为不存在的场景写错误处理
- 改动量与目标对得上;每改一行都能追到目标的某一部分
- 风格匹配现有代码,不"顺手优化"无关代码

## Output contract
- 改动后的文件清单 + 每个文件的核心变更点
- 一句话**怎么验证**(对应 Inputs 里的目标验收方式)
- 一行**没做什么**(显式列出被克制掉的东西,证明 YAGNI)
````

- [ ] **Step 3: 自查**

Run:
```bash
F=skills/write-code/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected: Lines ≤ 80,Sections = 5,Constraints 在 3-7 之间(本任务为 4)。

---

## Task 5: 写 review SKILL.md

**Files:**
- Create: `skills/review/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/review`

- [ ] **Step 2: 写 SKILL.md**

写入 `skills/review/SKILL.md`:

````markdown
---
name: review
description: Use when judging whether an existing artifact (code, design, doc, plan, prose) achieves its stated goal and respects its named constraints. Returns verdict with evidence — does not rewrite.
---

## When to invoke
- 已有产物 + 当时的目标和约束(没有就先拿到,否则 review 没意义)
- 用户问"这个行不行""有没有问题"
- 在合并、发布、提交前

## Inputs required
- 待审产物(代码 diff / 设计稿 / 文档 / 计划)
- 当时的**目标**与**约束**(必须明确,缺则要求补)

## Goal
产出一个判断: 产物是否达成目标、是否守住约束。判断必须基于证据(指向产物里的具体位置)而非主观偏好。

## Hard constraints
- 不重写产物;只产出 verdict + 证据
- 每条意见必须指向产物里的具体位置(行号、段名、组件名)
- 不带入"我会怎么写"的偏好;只对"是否达成目标 + 是否守约束"发声
- 区分**必须改**(违反约束/未达目标)与**建议**(改进空间);两者不混

## Output contract
- 总判断: PASS / NEEDS CHANGES / FAIL,一句话理由
- **必须改**清单(按严重度排序,每项含位置 + 违反了哪条约束/目标)
- **建议**清单(可选,每项含位置 + 改进点)
- 一行**复审条件**(达成什么状态可以再来)
````

- [ ] **Step 3: 自查**

Run:
```bash
F=skills/review/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected: Lines ≤ 80,Sections = 5,Constraints 在 3-7 之间(本任务为 4)。

---

## Task 6: 写 debug SKILL.md

**Files:**
- Create: `skills/debug/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/debug`

- [ ] **Step 2: 写 SKILL.md**

写入 `skills/debug/SKILL.md`:

````markdown
---
name: debug
description: Use when something is broken — error, crash, unexpected behavior, failing test. Locates the actual root cause; does not patch symptoms or modify code until the root cause is named.
---

## When to invoke
- 出现错误、崩溃、与预期不一致的行为
- 测试失败但实现"看起来"对
- 用户报告"它不工作"且伴随具体现象

## Inputs required
- 现象描述(看到了什么,期望什么)
- 可复现路径(没有就先建一条,这是 debug 的前置条件)
- 相关代码 / 日志 / 错误堆栈

## Goal
找到 bug 的真正根因(为什么这个现象会发生),并给出最小修复方案。根因必须能解释现象的所有方面,不只是"让它消失"。

## Hard constraints
- 不修改代码直到根因被一句话命名
- 不用 catch / swallow / retry 让现象消失
- 不在没有可复现路径时就提"修复"
- 区分**根因**与**触发条件**;最终方案对准根因

## Output contract
- 一句话**根因**(为什么会发生)
- 一句话**为什么之前没暴露**
- **最小修复**: 改哪个文件、改什么
- **回归测试**: 加什么测试可以阻止此问题再次出现
````

- [ ] **Step 3: 自查**

Run:
```bash
F=skills/debug/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected: Lines ≤ 80,Sections = 5,Constraints 在 3-7 之间(本任务为 4)。

---

## Task 7: 写 design-ui SKILL.md

**Files:**
- Create: `skills/design-ui/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/design-ui`

- [ ] **Step 2: 写 SKILL.md**

写入 `skills/design-ui/SKILL.md`:

````markdown
---
name: design-ui
description: Use when building any user interface, component, page, or visual layout. Produces a design with a committed aesthetic and explicit functional purpose — not generic defaults dressed in a framework.
---

## When to invoke
- 要产出新的界面、组件、页面、视觉布局
- 已有 UI 需要改样式 / 重排 / 加状态
- 在 write-code 写 UI 之前(目的是先确定形,再写代码)

## Inputs required
- 这个界面的**功能目的**(用户来这里要做什么)
- 适用场景与约束(屏幕尺寸、品牌色、必须复用的组件库)
- 1-3 个参考(可以是产品截图、风格关键词)

## Goal
产出一份有明确审美方向 + 明确功能行为的界面方案。方案能直接被 write-code 当输入消费,不需要再问"这里应该用什么颜色 / 间距 / 状态"。

## Hard constraints
- 必须明确陈述**审美选择**(不写"现代简洁"这种无意义词)
- 不依赖 UI 框架默认值搪塞(默认值 = 没设计)
- 每个交互状态(hover/active/disabled/error/loading/empty)必须列出
- 不为不存在的场景设计(YAGNI: 没要求的暗黑模式 / 国际化 / 无障碍可以提但不实现)

## Output contract
- **审美陈述**: 这个界面长什么样 + 为什么这么选(2-3 句)
- **布局**: 文字描述或低保真线框
- **每个组件的所有状态**清单
- **token 表**(颜色 / 字号 / 间距,如果项目有 token 系统则用,无则现起)
````

- [ ] **Step 3: 自查**

Run:
```bash
F=skills/design-ui/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected: Lines ≤ 80,Sections = 5,Constraints 在 3-7 之间(本任务为 4)。

---

## Task 8: 写 read SKILL.md

**Files:**
- Create: `skills/read/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/read`

- [ ] **Step 2: 写 SKILL.md**

写入 `skills/read/SKILL.md`:

````markdown
---
name: read
description: Use when given any URL, PDF, paper, or long document and the goal is to extract its content into reusable notes — not to summarize it down. Preserves information density, retains source pointers.
---

## When to invoke
- 用户给出 URL / PDF / 论文链接,要"看一下""读一下""记下要点"
- 准备 learn 或 write-prose 之前需要消化外部材料
- 不是要"总结"(总结会丢密度) — 要的是高密度可复用笔记

## Inputs required
- 至少 1 个 URL / 文件路径 / 文档标题
- (可选)关注的角度("我关心 X 方面")

## Goal
产出一份高密度笔记: 保留原文的关键事实、定义、数据、论证链路,而非压缩成摘要。笔记必须可以脱离原文被引用、被 learn / write-prose 当输入消费。

## Hard constraints
- 不"总结"(损失密度);保持原文的术语、数字、引文
- 每个非显然的论断必须**保留来源指向**(段落 / 章节 / 时间戳)
- 不加入个人解读 / 评价 / 联想(那是 learn / write-prose 的事)
- 不"翻译"成自己的话来"通顺化" — 关键术语保持原文

## Output contract
- 笔记主体: 按原文结构组织(章节标题对齐)
- 每节包含: 关键事实、定义、数据、引文(带原位置标注)
- 一行**未读完 / 未消化的部分**(诚实标注)
- 一行**关键引文 1-3 条**(原文摘抄,可直接用)
````

- [ ] **Step 3: 自查**

Run:
```bash
F=skills/read/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected: Lines ≤ 80,Sections = 5,Constraints 在 3-7 之间(本任务为 4)。

---

## Task 9: 写 write-prose SKILL.md

**Files:**
- Create: `skills/write-prose/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/write-prose`

- [ ] **Step 2: 写 SKILL.md**

写入 `skills/write-prose/SKILL.md`:

````markdown
---
name: write-prose
description: Use when explicitly asked to write or polish prose in Chinese or English. Strips AI writing patterns and produces text with an author's voice — not LLM defaults dressed up. Not for code comments, commit messages, or inline docs.
---

## When to invoke
- 用户明确要求"写""润色""改一段文字"
- 要产出公开发布的文字(文章、邮件、说明)
- 不是写代码注释、commit message、内联文档(那些用清晰直白即可,不需要 prose 风格)

## Inputs required
- 写什么(主题 / 一句话粗想法)
- 给谁看 + 在哪发(场景决定语气)
- (可选)风格参考: 喜欢哪种作者、哪种文章

## Goal
产出有作者声音、自然不像 AI 的中文 / 英文。读者读完不会立即识别为 LLM 输出。

## Hard constraints
- 删掉 AI 套话: "在当今" / "随着...的发展" / "总之" / "值得注意的是" / "深入探讨" / 大量并列三件套
- 不用 emoji 装饰、不用全文加粗、不用过度结构化标题层级
- 不写"显得专业"的废话(空洞的过渡句、车轱辘话)
- 长短句必须有节奏变化 — 不全是中长句堆叠

## Output contract
- 成文(可直接发布质量)
- 一行**风格说明**(声音定位 + 参考)
- (可选)**保留的反常用法**(故意为之的非标准表达,告诉用户不是错误)
````

- [ ] **Step 3: 自查**

Run:
```bash
F=skills/write-prose/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected: Lines ≤ 80,Sections = 5,Constraints 在 3-7 之间(本任务为 4)。

---

## Task 10: 写 learn SKILL.md

**Files:**
- Create: `skills/learn/SKILL.md`

- [ ] **Step 1: 创建目录**

Run: `mkdir -p skills/learn`

- [ ] **Step 2: 写 SKILL.md**

写入 `skills/learn/SKILL.md`:

````markdown
---
name: learn
description: Use when entering an unfamiliar domain and the goal is to build understanding through producing — articles, tutorials, working artifacts — rather than passive reading. Produces an output that anchors the learning, not a summary of sources.
---

## When to invoke
- 进入不熟悉的领域(技术 / 学术 / 行业 / 工艺)
- 已有多份 read 笔记或材料,需要整合成自己的理解
- 不是"快速扫一眼"(那是 read);要的是"建立可被自己复述的理解"

## Inputs required
- 学习领域(一句话)
- (强烈建议)≥2 份 read 笔记或权威材料
- **目标产出形式**: 文章 / 教程 / 可运行 demo / 内部分享 (没有就先定一个)

## Goal
通过"产出"建立对该领域的可复述理解。最终交付的不是材料摘要,而是一份独立成立的产物(文章/教程/demo),其中体现了对这个领域核心概念的把握。

## Hard constraints
- 必须有具体产出形式(没有产出锚点 = 没在 learn,在 read)
- 不以"摘抄"或"翻译"代替理解 — 每个核心概念用自己的话讲一遍
- 不假装懂没懂的东西 — 没把握的部分显式标"未消化"
- 不在材料密度不足时硬产出(< 2 份独立来源直接说"先 read")

## Output contract
- **产物**(按 Inputs 里指定形式 — 文章 / 教程 / demo)
- 一段**核心概念地图**(这个领域的 5-10 个关键概念 + 它们的关系)
- 一行**未消化的部分**(诚实标注)
- 一行**下一步**(继续学什么、用什么)
````

- [ ] **Step 3: 自查**

Run:
```bash
F=skills/learn/SKILL.md
echo "Lines: $(wc -l < $F)"
echo "Sections: $(grep -c '^## ' $F)"
echo "Constraints: $(awk '/^## Hard constraints/,/^## Output contract/' $F | grep -c '^- ')"
```
Expected: Lines ≤ 80,Sections = 5,Constraints 在 3-7 之间(本任务为 4)。

---

## Task 11: 写 README.md

**Files:**
- Create: `README.md`

- [ ] **Step 1: 写 README**

写入 `README.md`:

````markdown
# Praxis

Nine skills for Claude Code that turn raw model capability into precision.

Each skill sets a clear **goal**, names **hard constraints**, and defines an **output contract**. The process is left to the model.

## Philosophy

Models are already strong at output. Without structure, that strength becomes generic, imprecise work. Praxis adds structure — but not by scripting steps. It does so by making the *what* and the *what-not* extremely clear, and trusting the model with the *how*.

If you want a framework that runs you through checklists, choose [superpowers](https://github.com/obra/superpowers). If you believe the model is competent and what it needs is sharp boundaries, try Praxis.

## The 9 skills

| Skill | What it produces |
|---|---|
| `think` | Verifiable goal + named constraints + exposed assumptions |
| `plan` | Ordered task sequence with independent acceptance per task |
| `write-code` | Minimum runnable code that satisfies the goal — nothing speculative |
| `review` | Verdict (PASS / NEEDS CHANGES / FAIL) with evidence-pointed findings |
| `debug` | Named root cause + minimal fix + regression test |
| `design-ui` | Committed aesthetic + functional behavior, ready for write-code |
| `read` | High-density notes that preserve original terms, numbers, citations |
| `write-prose` | Chinese / English text with an author's voice, AI patterns stripped |
| `learn` | A produced artifact (article / tutorial / demo) that anchors understanding |

## Skill relationships

```
think → plan → write-code / debug / design-ui / write-prose / learn
                ↑___________________ review _________________↓
                                    read (independent)
```

- `think` runs before any work where the goal isn't already verifiable.
- `plan` runs after `think` when the work needs ordered steps.
- `review` validates downstream artifacts.
- `read` is independent — it feeds materials to `learn` and `write-prose`.

## Install

```bash
# Once Claude Code marketplace integration is wired up:
/plugin install praxis

# Until then, clone locally:
git clone https://github.com/<待定>/praxis ~/.claude/plugins/praxis
```

## Skill anatomy

Every skill follows the same five-section shape, ≤ 80 lines:

```
---
name: <slug>
description: <one line for model triggering>
---

## When to invoke         When this skill applies
## Inputs required        What must exist before starting
## Goal                   The state to reach (not the actions)
## Hard constraints       3-7 rigid red lines (must / must not)
## Output contract        Verifiable deliverables
```

## Status

v0.1 — all 9 skills present in their five-section shape, `think` written to full depth as the template. Other 8 skills will deepen as I use them in real work.

## License

MIT
````

- [ ] **Step 2: 自查 — 9 个 skill 全部出现在表里**

Run:
```bash
for s in think plan write-code review debug design-ui read write-prose learn; do
  grep -q "\`$s\`" README.md && echo "OK: $s" || echo "MISS: $s"
done
```
Expected: 9 行全部 `OK: <slug>`。

---

## Task 12: 本地装载验证

**Files:** 无新文件

- [ ] **Step 1: 验证目录结构完整**

Run:
```bash
echo "plugin.json: $(test -f .claude-plugin/plugin.json && echo OK || echo MISS)"
for s in think plan write-code review debug design-ui read write-prose learn; do
  echo "$s: $(test -f skills/$s/SKILL.md && echo OK || echo MISS)"
done
echo "README: $(test -f README.md && echo OK || echo MISS)"
```
Expected: 所有行 `OK`。

- [ ] **Step 2: 验证每个 SKILL.md 的 frontmatter 合法**

Run:
```bash
for f in skills/*/SKILL.md; do
  head -1 "$f" | grep -q '^---$' || echo "BAD frontmatter start: $f"
  awk '/^---$/{c++} c==2{exit} END{if(c<2) print "BAD frontmatter end: " FILENAME}' "$f"
done
echo "(no BAD lines = all OK)"
```
Expected: 无 `BAD` 输出。

- [ ] **Step 3: 验证所有 description 字段存在且非空**

Run:
```bash
for f in skills/*/SKILL.md; do
  d=$(awk '/^---$/{c++; next} c==1 && /^description:/{print; exit}' "$f")
  [ -n "$d" ] && echo "OK: $f" || echo "MISS description: $f"
done
```
Expected: 所有行 `OK: ...`。

- [ ] **Step 4: 装到 Claude Code 试触发(用户手动)**

这一步需要用户操作:
1. 在 Claude Code 里 `/plugin add /path/to/Praxis`(或对应命令)
2. 开新会话,输入"我想做个 X"(模糊需求),期望 think skill 被触发
3. 输入"读一下 https://example.com"(URL),期望 read skill 被触发
4. 任意触发失败的 skill 需要回到对应任务调整 description

验证后 plan v0.1 完成。

---

## Self-Review

完成 plan 编写后做的自检结果:

**1. Spec coverage** —

| Spec 节 | 覆盖任务 |
|---|---|
| 1. 定位与哲学 | Task 11 (README 哲学陈述) |
| 2. 9 个 skill 清单 | Task 2-10 |
| 3. Skill 骨架规范 | Task 2-10(每个任务自查 5 段 + 行数 + Hard constraints) |
| 4. 样板 think | Task 2(直接采用 spec 里的内容) |
| 5. 命名规则 | Task 2-10 的 slug 与 spec 一致 |
| 6. Plugin 目录结构 | Task 1 + Task 2-10(目录创建步骤) |
| 7. MVP 三阶段 | Task 2 = 阶段 1;Task 3-12 = 阶段 2;阶段 3 不在本 plan |
| 8. 发布定位 | Task 11 README 写明 v0.1 状态;实际发布在用户 git init + push 后 |
| 9. 待解决问题 | 无 |

无遗漏。

**2. Placeholder 扫描** —
- `homepage` 的 `<待定>` 在 Task 1 + Task 11 出现,这是显式标注的待用户填项,不算 placeholder
- 无 TBD / TODO / "实现略" / "类似上面"

**3. Type / 命名一致性** —
- 9 个 slug 在 plugin.json / SKILL.md frontmatter / README 表 / 自查脚本 / 关系图中拼写一致
- 自查脚本命令在 Task 2-10 完全相同(只换文件路径),复制粘贴可用
- 5 段名称在每个 SKILL.md 中拼写完全相同(When to invoke / Inputs required / Goal / Hard constraints / Output contract)
- 关系图在 spec 第 2 节、Task 11 README 中一致(都画 read 独立)

无不一致。
